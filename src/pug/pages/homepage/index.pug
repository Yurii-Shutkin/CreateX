include ../../_paths.pug

doctype html
html(lang="en")
    head
        meta(charset="UTF-8")
        meta(name="viewport", content="width=device-width, initial-scale=1.0")
        link(rel="stylesheet", href=`${paths.css}`)
        title Document
    body 
        div.container
            include ../../partials/components/header.pug
        script(type="module", src=`${paths.js}/homepage/main.js`) 
